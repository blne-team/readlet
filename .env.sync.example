# Copy this file to .env.sync. Never commit the copy or its values.
# Create a Cloudflare Access service token and allow it through the Access
# application protecting the Readlet Worker.
READLET_SYNC_ACCESS_CLIENT_ID=your-service-token-client-id
READLET_SYNC_ACCESS_CLIENT_SECRET=your-service-token-client-secret
